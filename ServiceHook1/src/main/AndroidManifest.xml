<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jianqiang.com.activityhook1">

    <application
        android:name=".UPFApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Bao服务测试"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".defineservice.StubService1" />
        <service android:name=".defineservice.StubService2" />
        <service android:name=".defineservice.StubService3" />
        <service android:name=".defineservice.StubService4" />

    </application>

</manifest>